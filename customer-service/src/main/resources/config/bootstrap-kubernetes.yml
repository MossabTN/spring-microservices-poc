spring:
  profiles:
    active: #spring.profiles.active#
  cloud:
    kubernetes:
      discovery:
        all-namespaces: false
      secrets:
        enable-api: true
    consul:
      enabled: false
