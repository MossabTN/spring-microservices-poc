spring:
  profiles:
    active: #spring.profiles.active#
  cloud:
    kubernetes:
      discovery:
        all-namespaces: false
        service-name: customer
      secrets:
        enable-api: true
    consul:
      enabled: false
