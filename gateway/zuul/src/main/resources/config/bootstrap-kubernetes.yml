spring:
  profiles:
    active: #spring.profiles.active#
  cloud:
    kubernetes:
      discovery:
        all-namespaces: true
      secrets:
        enable-api: true
    consul:
      enabled: false
